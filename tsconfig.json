{
    "compilerOptions": {
        //  as 로  가오는거 방지
        "esModuleInterop" : true,
        //  에러난 위치 찾아주기
        "sourceMap" : true,
        // 최신문법, 프론트개발 = DOM
        "lib" : ["ES2020","DOM"],
        // jsx = react꺼다 라고 인식
        "jsx" : "react",
        "module" : "ESNext",
        "moduleResolution": "Node",
        "target" : "ES5",
        // 이건 꼭 켜줘야함 안 켜주면 any 남발 할 수 있음
        "strict" : true,
        "resolveJsonModule" : true,
        "baseUrl" : ".",
        "downlevelIteration": true, /* 'ES5'또는 'ES3'를 대상으로 할 때 'for-of', 스프레드 및 디스트 럭처링에서 이터 러블을 완벽하게 지원합니다. */
        "paths": {
            "@hooks/*" : ["hooks/*"]
            ,"@components/*" : ["components/*"]
            ,"@layouts/*" : ["layouts/*"]
            ,"@util/*" : ["utils/*"]
            ,"@apis/*" : ["apis/*"]
            ,"@pages/*" : ["pages/*"]
            ,"@common/*" : ["common/*"]
        }
    }
}